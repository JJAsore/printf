#include <stdio.h>
#include "main.h"

/**
  * specifier - check character specifier and appropriate function
  *for printing
  * @format:the specifier type(char *)
  *
  * Return: pointer to a function.
  */

int (*specifier(char*))(va_list)
{
	
}
